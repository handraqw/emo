# Core numerical stack
numpy==1.26.4
scipy==1.13.1
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.2
tqdm==4.66.5

# Video / UI backends (optional at runtime; code falls back to heuristics if unavailable)
opencv-python==4.10.0.84
mediapipe==0.10.14
PySide6==6.7.2

# Audio / speech backends (optional at runtime; code falls back to metadata-driven stubs if unavailable)
vosk==0.3.45
librosa==0.10.2.post1
soundfile==0.12.1
imageio-ffmpeg==0.6.0

# Model training / inference targets
torch==2.6.0
torchaudio==2.6.0
onnx==1.17.0
onnxruntime-gpu==1.19.2
